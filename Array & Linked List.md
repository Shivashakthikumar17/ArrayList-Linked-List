==> Array List :
 Resizeable array Implementaion.
 Storing the values in side by side.
 Fast and Random access.->0(1)
 Insertion and deletion is slow -> 0(n)
 High Memory Access because when user create Arraylist means it will create more set of memory.

 ==> Linked List : 
  Its store the element in individual node and those nodes are scattered .
  Every node contains two values : 
              1.Actual data
              2.Pointer ->refers next node.
  Insertion and deletion is fast ->0(1)
  Accessing element is very slow due to pointers.
